import { Injectable } from '@nestjs/common';

@Injectable()
export class Uploadfile001Service {
    // constructor(private readonly ){}

    // async savedata(order_id : number){
    //     return await this.
    // }
}
